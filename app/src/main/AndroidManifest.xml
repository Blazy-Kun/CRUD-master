<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.crud">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LecturerSessionHistory"></activity>
        <activity android:name=".Info" />
        <activity
            android:name=".Admin"
            android:screenOrientation="landscape"></activity>
        <activity android:name=".Profile" />
        <activity android:name=".SendMessage" />
        <activity android:name=".Attendance" />
        <activity android:name=".PaymentHistory" />
        <activity android:name=".SessionHistorry" />
        <activity android:name=".BookSession" />
        <activity android:name=".LecturerDashBoard" />
        <activity android:name=".StudentDash" />
        <activity android:name=".Welcome" />
        <activity android:name=".Voice" />
        <activity android:name=".SignIn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Register" />

        <service
            android:name=".MyService"
            android:enabled="true"
            android:exported="true" />
    </application>

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

</manifest>